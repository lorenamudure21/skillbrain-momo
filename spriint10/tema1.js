//am definit o funcție numita square care accepta un parametru numit num.
function square(num) {
// cu acest let se calcula patratul numarului num prin inmultirea lui num cu el insusi.
let result = num * num; 
//aici cu acest return vom returna rezultatul calculat, care va fi patratul numarului.
        return result;
}  
console.log(square(8)); 